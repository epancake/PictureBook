import { MoveableDirective } from './moveable.directive';

describe('MoveableDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveableDirective();
    expect(directive).toBeTruthy();
  });
});
